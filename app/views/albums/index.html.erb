<h1>HI!</h1>

<% if @user && current_user.id == @user.id %>
  <a href="<%= new_user_album_url(current_user) %>">Add an Album</a>
<% end %>

<br>

<ul>
  <% @albums.each do |album| %>
    <a href="<%= album_url(album) %>"><li> <%= album.title %> </li></a>
  <% end %>
</ul>
